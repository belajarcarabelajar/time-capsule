{
  "name": "time-capsule",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "deploy": "turbo run build && wrangler pages deploy apps/web/dist --project-name time-capsule"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "bun@1.2.0"
}
